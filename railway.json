{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "PACK"
  },
  "deploy": {
    "startCommand": ". /opt/venv/bin/activate && cd backend && gunicorn --bind 0.0.0.0:$PORT app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
